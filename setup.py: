from setuptools import setup, find_packages

setup(
    name='smart_property_assessment',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        'fastapi',
        'uvicorn',
        'azure-ai-ml',
        'azure-functions',
    ],
)
